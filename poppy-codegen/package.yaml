name: poppy-codegen
version: 0.1.0.0
github: "hnerdrum/poppy"
license: BSD3
license-file: LICENSE
author: "Henrik Nerdrum"
maintainer: "henrik.nerdrum@gmail.com"
copyright: "2023–2026 Henrik Nerdrum"
category: Database
synopsis: Schema builder and Client codegen for Poppy
description: Codegen library for Poppy. Describe models as Haskell values, emit table types and a Client, and drift-check against live Postgres.

extra-source-files:
  - test/migrations/*.sql
  - test/Poppy/Codegen/golden/*.golden

dependencies:
  - base >= 4.7 && < 5
  - containers
  - directory
  - filepath
  - poppy
  - postgresql-simple
  - text

ghc-options:
  - -Wall
  - -Wcompat
  - -Widentities
  - -Wincomplete-record-updates
  - -Wincomplete-uni-patterns
  - -Wmissing-export-lists
  - -Wmissing-home-modules
  - -Wpartial-fields
  - -Wredundant-constraints

library:
  source-dirs: src
  exposed-modules:
    - Poppy.Codegen.CLI
    - Poppy.Codegen.Drift
    - Poppy.Codegen.IR
    - Poppy.Codegen.Run
    - Poppy.Codegen.Schema
    - Poppy.Codegen.Target
    - Poppy.Codegen.Validate
  other-modules:
    - Poppy.Codegen.Combinator
    - Poppy.Codegen.Emit.Client
    - Poppy.Codegen.Emit.Combinator
    - Poppy.Codegen.Emit.FlatRow
    - Poppy.Codegen.Emit.Include
    - Poppy.Codegen.Emit.Nest
    - Poppy.Codegen.Emit.Schema
    - Poppy.Codegen.Emit.SelectIn
    - Poppy.Codegen.EmitCommon
    - Poppy.Codegen.IncludePath
    - Poppy.Codegen.Introspect
    - Poppy.Codegen.JoinAlias
    - Poppy.Codegen.Lookup
    - Poppy.Codegen.TextUtil
  default-extensions:
    - NamedFieldPuns
    - OverloadedRecordDot
    - OverloadedStrings

# Compile library sources with the test suite so hidden Emit/JoinAlias/IncludePath
# modules can be unit-tested without exposing them to consumers.
tests:
  poppy-codegen-test:
    main: Spec.hs
    source-dirs:
      - test
      - src
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - poppy
      - bytestring
      - hspec
      - mtl
      - resource-pool
      - time
      - uuid
    default-extensions:
      - AllowAmbiguousTypes
      - NamedFieldPuns
      - OverloadedRecordDot
      - OverloadedStrings
      - TypeApplications
